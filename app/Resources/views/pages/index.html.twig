

{{ include('pages/layout/header.html.twig') }}


    <div class="container">
        <div>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>


{{ include('pages/layout/footer.html.twig') }}